<form name="editService">
  <fieldset>
    <legend>
      Edit Service
    </legend>
      <div class="control-group">
        <label class="control-label">
          Name
        </label>
        <div class="controls">
            <input type="text"
                   name="service[name]"
                   ng-model="service.name"
                   class="text_field"
                   required>
          <span ng-show="editService.name.$error.required"
                class="help-inline">
            Required
          </span>
        </div>
      </div>
    <div class="form-actions">
      <button class="btn btn-success" ng-click="save()"
        ng-disabled="isClean() || editService.$invalid">
        Save
      </button>
      <button ng-click="destroy()"
              ng-show="service.id"
              class="btn btn-danger">
              <i class="icon-trash"></i>Delete
      </button>
      <a href="/services" class="btn">
        Back to services
      </a>
    </div>
  </fieldset>
</form>

